/*
 * LoginPage Messages
 *
 * This contains all the text for the LoginPage component.
 */
import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.containers.ExpenseOverviewPage';

export default defineMessages({
  appName: {
    id: `${scope}.start_project.appName`,
    defaultMessage: '7 Cents',
  },
});
